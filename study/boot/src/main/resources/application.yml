spring:
  datasource:
    url: jdbc:mysql://localhost:3306/teamsky?useSSL=false&allowPublicKeyRetrieval=true
    username: teamsky
    password: teamsky
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
